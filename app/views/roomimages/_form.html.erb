<%= form_with(model: roomimage) do |form| %>
  <% if roomimage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(roomimage.errors.count, "error") %> prohibited this roomimage from being saved:</h2>

      <ul>
        <% roomimage.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :avatar_path %>
    <%= form.text_field :avatar_path %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
